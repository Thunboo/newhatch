# Project Prompt

Проектный промпт для повторного использования.

## Default

Read and follow `AGENTS.md`, which owns context routing and fixed project guardrails. Use `.codex/context.md` for quick orientation and open only the focused canonical documents relevant to the task. Before saving context, synchronize affected mirrors through `.agents/memory/documentation-sync.md`.
