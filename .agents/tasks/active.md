# Active Tasks

No implementation task is currently active.

## Recently Completed

- Collector/analyzer split with local and remote analyzer modes.
- IP/FQDN split endpoints and optional collector allowlist.
- Single-user authentication and CIDR-restricted nginx ingress.
- Live merged Sessions feed with paused polling in history and automatic older-page loading.
- Remote collector status indicator.
- Selected-session row outline, fixed desktop sidebar, independent detail/list scrolling and `Escape` close.

## Operational Follow-Up

- Keep the VPS collector image rebuilt from current sources when deploying FQDN support.
- Perform final competition-network validation on the actual target Linux/vulnbox topology when available.

Future implementation work should be selected from `.agents/tasks/backlog.md` and moved here before coding.
