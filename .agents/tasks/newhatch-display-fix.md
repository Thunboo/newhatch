User:
```
У нас есть проблема в отображении собранных пакетов. Пример:
GET /admin/health-check?url=http%3A%2F%2F127.0.0.1%3A3000%2Fapi%2Fnews%2F4 HTTP/1.1
Host: thunboo-vps-nl.thunboo.dev:3000
User-Agent: Mozilla/5.0 (compatible; CTF-Exploit/1.0)
Accept-Encoding: gzip, deflate, zstd
Accept: */*
Connection: keep-alive

{
"reachable": true,
"response": "{\n  "author\_id": 4,\n  "category": "\u0411\u0435\u0437\u043e\u043f\u0430\u0441\u043d\u043e\u0441\u0442\u044c \u0441\u0442\u0430\u043d\u0446\u0438\u0439",\n  "content": "FLAG{FLAG\_TEST\_STOLEN\_x3000}",\n  "created\_at": "2026-09-20T14:59:18.407732",\n  "id": 4,\n  "is\_private": false,\n  "title": "title\_test\_1"\n}\n",
"status": "success",
"url": "[http://127.0.0.1:3000/api/news/4](http://127.0.0.1:3000/api/news/4)"
}

Надо сделать так, чтобы кодировка - была принята и подставлен нужный символ - это первое. А второе - в окне подробного просмтра сессии - добавь кнопку копирования отдельно C2S запроса и S2C ответа, чтобы пользователь мог удобно копировать одним кликом. А еще в этом просмотре сессии подставь чекбокс - форматировать json - чтобы тогда существующйи в запросе или ответе json форматиловался как при использовании jq - форматирование json в выводе - чисто фронт

Да и по сути - вся эта задача - в основном фронт, а не бекенд.&#x20;

По классике - сначала твои вопросы и предложения, потом переходим к коду
```

Agent:
```
Понял задачу. Предлагаю оставить исходные payload-байты неизменными, а преобразования делать только в отображении фронтенда.

Мои вопросы:

1. Нужно декодировать только JSON-экранирование (`\u0411` → `Б`, `\n`, `\"`) или также URL encoding в request line (`%3A%2F%2F` → `://`)? Я предлагаю отображать декодированными оба варианта.

2. При включённом `Format JSON` предлагаю сохранять HTTP start line и headers как есть, а через форматирование уровня `jq` пропускать только body. Если JSON находится строкой внутри другого JSON, как поле `response` в примере, нужно ли превращать его во вложенный объект? Я бы сделал это: распознавать JSON-строки рекурсивно с небольшим ограничением глубины.

3. Что должна копировать кнопка: точный исходный HTTP-поток или преобразованный текст, который сейчас видит пользователь? Я рекомендую точный исходный C2S/S2C: он пригоден для воспроизведения запроса и не изменён форматированием.

Предлагаемый UI:

- отдельная кнопка с иконкой Copy в заголовке `Client → server`;
- такая же кнопка в `Server → client`;
- после копирования кратковременно показывать Check;
- checkbox `Format JSON` в toolbar, активный только в режиме Text;
- декодирование Unicode и URL выполняется только визуально;
- Hex остаётся точным представлением исходных байтов;
- ошибки JSON parsing не ломают просмотр: поток показывается обычным текстом.

Отдельно: `Content-Encoding: gzip/br/zstd` — это уже сжатие, а не JSON-кодировка. Его автоматическую распаковку лучше считать отдельной задачей, если такие ответы реально встречаются.
```

User:
```
1. Да, оба варианта
2. согласен, jq только к body + распознавание (это же не настолько ресурсо-затратно. происходит только при открытии детального просмотра сессии)
3. копировать все, что в окнах c2s / s2c есть

Предлагаемый тобой ui - меня устраивает
```