server {
    listen 18080;
    server_name _;
    keepalive_timeout 0;

    location = /flag {
        alias /usr/share/nginx/html/flag.json;
        default_type application/json;
        add_header Cache-Control "no-store" always;
    }

    location / {
        return 404;
    }
}
