server {
    listen @PORT@;
    listen [::]:@PORT@;
    server_name _;
    @ALLOW_RULES@
    client_max_body_size 4k;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy same-origin always;
    add_header Cache-Control "no-store" always;
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' data:; style-src 'self'; frame-ancestors 'none'; base-uri 'none'; form-action 'self'" always;

    proxy_http_version 1.1;
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP "";
    proxy_set_header X-Forwarded-For "";
    proxy_set_header Forwarded "";

    root /usr/share/nginx/html;
    index index.html;

    location = /api/health {
        allow all;
        if ($request_method != GET) { return 405; }
        proxy_pass http://127.0.0.1:3000;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
    }

    # Defense in depth: these artifacts are not part of the frontend image and
    # must never become downloadable if a future image or mount is misconfigured.
    location ~ (^|/)\. {
        return 404;
    }

    location ~* (^|/)(data|segments)(/|$) {
        return 404;
    }

    location ~* \.(sqlite|sqlite3|db|seg)(/|$) {
        return 404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
